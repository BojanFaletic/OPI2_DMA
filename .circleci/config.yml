version: 0.1

jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01
    working_directory: ~/project
    steps:
      - sudo apt install clang
      - checkout
      - run: mkdir build build/obj
      - run: make CC=clang++